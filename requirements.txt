# Dépendances Python installées via pip.
# La bibliothèque « graphviz » nécessite aussi le binaire système Graphviz (commande « dot »)
# pour générer correctement les rendus JPEG.
streamlit>=1.33,<2.0
pandas>=2.2,<3.0
graphviz==0.20.3
Pillow>=9.5,<11.0
# spaCy avec pipeline transformer : l'extra "[transformers]" n'est pas
# toujours interprété par pip sur certaines plateformes (ex. Streamlit Cloud).
# On force donc explicitement l'installation du module spaCy Transformers.
spacy==3.7.4
spacy-transformers==1.3.4
# Dépendances CLI spaCy
typer==0.9.0
click==8.1.7
# Backend transformer PyTorch
torch==2.3.1
# Modèle français transformeur compatible spaCy 3.7.x
# Si la 3.7.2 404, essaie 3.7.1 ou 3.7.0 (même motif d'URL).
fr_core_news_trf @ https://github.com/explosion/spacy-models/releases/download/fr_core_news_trf-3.7.2/fr_core_news_trf-3.7.2-py3-none-any.whl
