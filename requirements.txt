# Les dépendances Python requises (Streamlit, pandas, graphviz, Pillow, altair, spaCy, etc.)
# sont déjà préinstallées dans l'environnement de déploiement utilisé par l'application.
# Le fichier est volontairement vide afin d'éviter les installations pip inutiles qui
# échouent hors-ligne. Si vous exécutez ce projet localement, installez les dépendances
# nécessaires selon votre environnement (ex. : `pip install streamlit pandas graphviz pillow altair spacy`).
