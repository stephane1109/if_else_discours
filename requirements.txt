# Dépendances Python installées via pip.
# La bibliothèque « graphviz » nécessite aussi le binaire système Graphviz (commande « dot »)
# pour générer correctement les rendus JPEG.
streamlit>=1.33,<2.0
pandas>=2.2,<3.0
graphviz==0.20.3
Pillow>=9.5,<11.0
altair>=5.3,<6.0
spacy==3.7.4
# Dépendances CLI spaCy
typer==0.9.0
click==8.1.7
# Modèle français standard (taille moyenne) compatible spaCy 3.7.x
fr_core_news_md==3.7.2
# Distribution pip officielle du modèle spaCy moyen (nécessaire pour « pip install -r requirements.txt »)
fr-core-news-md==3.7.2
